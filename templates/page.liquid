SUP MY CHUBIES!!!!
{% render 'component-header', title: page.title %}

{% capture tile-content %}
    {{ page.content }}
{% endcapture %}

{% render 'component-tile', tile-type: 'large', tile-content: {{tile-content}} %}

{% render 'component-cta' %}
