{{ block.settings.html_embed }}

{% schema %}
{
  "name": "HTML Embed",
  "settings": [{ "type": "html", "id": "html_embed", "label": "HTML Embed" }],
  "presets": [{ "name": "HTML Embed" }]
}
{% endschema %}
